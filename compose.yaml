services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=db'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=admin'
      - 'MYSQL_PASSWORD=admin'
    ports:
      - '3306'
